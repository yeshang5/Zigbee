#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../cnpm/bin/cnpm" "$@"
  ret=$?
else 
  node  "$basedir/../cnpm/bin/cnpm" "$@"
  ret=$?
fi
exit $ret
