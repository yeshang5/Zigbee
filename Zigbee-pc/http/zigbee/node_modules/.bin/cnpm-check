#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../cnpm/bin/cnpm-check" "$@"
  ret=$?
else 
  node  "$basedir/../cnpm/bin/cnpm-check" "$@"
  ret=$?
fi
exit $ret
